I dunno how to use README's yet so...


But Here are the build instructions.

**Steps**
    1. Run ``make all`` which will build the operating system and place it inside the build directory as 'myos.iso'
    2. Run ``make run`` to run it in qemu (or use another emulator of your choice as long as it supports Protected Mode and uses VGA)